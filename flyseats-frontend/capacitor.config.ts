import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flyseats.app',
  appName: 'flyseats',
  webDir: 'dist/flyseats-frontend'
};

export default config;
